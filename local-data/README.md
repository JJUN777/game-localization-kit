# 로컬 데이터

이 폴더는 Git에 올리지 않는 대용량 원본과 생성 결과를 보관합니다.

`legacy/`에는 통합 CLI 이전의 `90_*`~`97_*`, `9999. final` 자료가 원래 폴더명 그대로 이동되어 있습니다. 신규 프로젝트 데이터는 이곳이 아니라 기본 `workspaces/<project_id>/`에 생성됩니다.

이 README를 제외한 `local-data/`의 파일은 다른 컴퓨터로 자동 동기화되지 않습니다.
