# LLM 사용량과 비용

이 문서는 GLK에서 AI API 호출이 발생하는 단계, 비용 계산 방법과 실제 프로젝트 기준 예시를 정리합니다.

**대상 독자**: 프로젝트를 시작하기 전 비용을 예측하거나, 비용을 줄이고 싶은 모든 사용자

---

## 한눈에 보는 비용

> 아래 금액 기준: `gemini-3.8-flash`, 1달러 = 1,550원, Gemini 무료 할당량 미적용, 정상 1회 처리. 2026년 12월 31일까지 적용되는 출시 기념 단가를 사용합니다.
>
> **Gemini 무료 티어 범위 안에서는 비용이 발생하지 않습니다.** 현재 프로젝트의 실제 한도는 [Google AI Studio](https://aistudio.google.com/)에서 확인합니다. 무료 한도는 모델과 계정에 따라 다를 수 있으므로 [공식 Rate limits](https://ai.google.dev/gemini-api/docs/rate-limits)도 참고합니다.
>
> 아래 금액과 실측 예시는 Gemini 전용입니다. OpenAI를 선택한 경우 모델별
> 입력·출력 단가가 다르므로 [OpenAI API 가격 안내](https://developers.openai.com/api/docs/pricing)에서 현재 단가를 확인하세요.

### 룰북 PDF (페이지당)

```text
영어 원문 약 1,000~1,500자 기준

PDF 읽기 순서 복원: 약 20~40원
초벌 한국어 번역:   약 20~40원
────────────────────────────
합계:               약 40~80원/페이지
```

| 룰북 분량 | 참고 총액 |
|---:|---:|
| 1페이지 | 약 40~80원 |
| 10페이지 | 약 400~800원 |
| 50페이지 | 약 2,000~4,000원 |
| 100페이지 | 약 4,000~8,000원 |

### 이미지 OCR (장당)

```text
thinking 제외 환산: 약 6원
기본 thinking 포함 예산: 약 6~24원
```

| 이미지 수 | thinking 제외 | thinking 포함 예산 |
|---:|---:|---:|
| 5장 | 약 30원 | 약 30~120원 |
| 100장 | 약 600원 | 약 600~2,400원 |
| 1,000장 | 약 6,000원 | 약 6,000~24,000원 |

### 비용이 없는 작업

원문 QA, 용어 후보 생성, termbase import, 번역 QA, 검수 화면의 일반적인
조회·편집·저장·최종 승인, 프로젝트 목록과 상태 확인은 모두 로컬 처리이므로
API 비용이 없습니다. 단, PDF 원문 검수 화면에서 사용자가 명시적으로 실행하는
`아이콘 검사(AI)`는 cache에 없는 선택 block마다 이미지 입력을 포함한 API 요청
한 번이 발생합니다. 동일한 입력과 설정으로 이미 검사한 block은 로컬 저장 결과를
재사용하므로 추가 비용이 발생하지 않습니다.

용어 검수의 `후보 1차 정리(AI)`도 선택 실행할 때만 비용이 발생합니다. 현재
`review`인 자동 후보를 최대 25개씩 묶어 요청하고, 후보별 fingerprint cache에
있는 결과는 요청에서 제외합니다. 실행 확인 창의 비용은 prompt 문자 수와 예상
출력 token 범위로 계산한 참고값이며 실제 사용량은 응답 metadata로 기록합니다.

번역 프롬프트의 `AI로 초안 만들기`는 최종 승인 원문의 대표 block을 한 요청에
모아 문체 지침을 제안합니다. 앞 4페이지의 연속 문맥은 최대 12,000자, 나머지
페이지의 규칙 문체 표본은 최대 8개·4,000자로 제한합니다. 실행 전에 예상
입력·출력 token과 비용을 표시하고,
실행 뒤에는 제공자가 반환한 실제 token과 비용을 표시합니다. 같은 승인 원문,
현재 프롬프트, 제공자·모델과 prompt version이면 저장된 초안을 즉시 보여주고
재사용합니다. `새로 만들기`를 선택하면 기존 cache가 있어도 예상 사용량 확인 후
새 API 요청과 비용이 발생합니다.

---

## 비용 기준 정보

이 문서의 모든 금액은 다음 기준으로 계산했습니다.

| 항목 | 값 |
|---|---|
| 기준일 | 2026년 9월 3일 |
| 모델 | `gemini-3.8-flash` (GLK 기본 모델) |
| 환율 | 1달러 = 1,550원 (참고용, 실제 환율과 다를 수 있음) |
| 요금 기준 | Gemini Developer API 표준 유료 요금 (세금·환전 수수료 제외) |

요금과 무료 할당량은 변경될 수 있으므로 정확한 금액은 [Gemini API 공식 요금표](https://ai.google.dev/gemini-api/docs/pricing)에서 확인합니다.

### 현재 기본 모델 단가

| 구분 | 100만 token당 |
|---|---:|
| 텍스트·이미지 입력 | $0.75 |
| 텍스트 출력과 thinking token | $3.75 |
| context cache 입력 | $0.075 |

위 단가는 2026년 12월 31일까지 적용됩니다. 2027년 1월 1일부터 공식 표준
단가는 입력 $1.50, 출력 $7.50, context cache 입력 $0.15로 변경될 예정입니다.

다른 모델을 `--model`로 지정하면 해당 모델의 요금이 적용됩니다.

---

## 비용이 발생하는 단계

| 작업 | LLM 호출 | 호출 단위 |
|---|---:|---|
| `glk init`, `glk projects`, `glk status` | 없음 | 로컬 처리 |
| PDF 텍스트·좌표 추출과 페이지 렌더링 | 없음 | 로컬 처리 |
| PDF 읽기 순서 복원 | 있음 | 캐시되지 않은 PDF 페이지마다 1회 |
| 이미지 OCR | 있음 | 캐시되지 않은 이미지마다 1회 |
| `segment`, 원문 QA와 일반 원문 검수 | 없음 | 로컬 처리 |
| 원문 검수의 `아이콘 검사(AI)` | 있음 | cache에 없는 선택 block마다 1회 |
| 용어 후보 생성과 termbase import | 없음 | 로컬 처리 |
| 용어 검수의 `후보 1차 정리(AI)` | 있음 | cache에 없는 자동 후보 최대 25개마다 1회 |
| 번역 프롬프트의 `AI로 초안 만들기` | 있음 | cache에 없는 대표 원문 묶음마다 1회 |
| 초벌 번역 | 있음 | 캐시되지 않은 번역 chunk마다 1회 |
| 번역 검수·QA·최종 승인 | 없음 | 로컬 처리 |
| QA 오류 block 선택 재번역 | 있음 | 재번역 대상 block마다 1회 |

번역 프롬프트 저장·직접 수정 자체는 API를 호출하지 않습니다. `AI로 초안
만들기`를 명시적으로 실행했을 때만 초안 요청 비용이 발생합니다. 프롬프트 변경 후
전체 재번역을 선택하면 기존 청크를 재사용하지 않으므로 모든 번역 chunk에도
비용이 다시 발생합니다.

`--dry-run`은 입력과 실행 가능 여부만 확인하며 AI API를 호출하지 않습니다.

---

## 계산식

```text
입력 비용 = 입력 token ÷ 1,000,000 × 입력 단가
출력 비용 = (표시 출력 token + thinking token) ÷ 1,000,000 × 출력 단가
총비용 = 입력 비용 + 출력 비용
```

이미지도 token으로 환산됩니다. Google은 큰 이미지를 768×768 tile로 나누고 tile마다 token을 계산합니다. 정확한 입력량은 API의 `count_tokens`로, 실제 청구 대상은 응답의 usage metadata로 확인할 수 있습니다.

아래 실측 예시는 기존 sample에서 측정한 입력·출력 token 수를 유지하고
Gemini 3.8 Flash의 현재 단가로 다시 환산한 값입니다. 3.8의 실제 thinking token은
문서와 요청 난이도에 따라 달라지므로 프로젝트 카드에 기록된 실사용량을 최종
기준으로 사용하세요.

참고 링크:
- [Token 계산과 count_tokens](https://ai.google.dev/gemini-api/docs/tokens)
- [Gemini API 결제 안내](https://ai.google.dev/gemini-api/docs/billing/)

---

## 실측 예시: PDF 읽기 순서 복원

PDF 룰북 측정 샘플의 렌더 이미지(787×1187px)와 실제 prompt를 `count_tokens`로 측정하고, 저장된 JSON 출력을 같은 tokenizer로 계산한 결과입니다.

| 페이지 | fragment 수 | 입력 token | 출력 추정 token | thinking 제외 비용 |
|---|---:|---:|---:|---:|
| 1 | 57 | 3,479 | 1,364 | 약 $0.0077 (≈12원) |
| 2 | 108 | 5,870 | 1,974 | 약 $0.0118 (≈18원) |

Gemini 3.8 Flash의 기본 thinking level은 `medium`입니다. thinking token을
1,000~3,000개로 가정하면 페이지당 약 18~36원이며, thinking token 1,000개마다
약 $0.00375(≈5.8원)가 추가됩니다.

---

## 실측 예시: 이미지 OCR

이미지 OCR 측정 샘플의 카드 이미지 5장(모두 932×1270px)과 아이콘 30종 상세 prompt를 `count_tokens`로 측정한 결과입니다.

| 항목 | 값 |
|---|---:|
| prompt token | 2,716 |
| 이미지 token | 259 |
| 장당 전체 입력 token | 2,974 |
| 기존 OCR 출력 token | 평균 394 (최소 341, 최대 445) |

| 구분 | 입력 비용 | 출력 비용 | 합계 | 원화 |
|---|---:|---:|---:|---:|
| 출력 최소 (341) | $0.002231 | $0.001279 | $0.003509 | 약 5.4원 |
| 출력 평균 (394) | $0.002231 | $0.001478 | $0.003708 | 약 5.7원 |
| 출력 최대 (445) | $0.002231 | $0.001669 | $0.003899 | 약 6.0원 |

현재 OCR은 `thinking_budget`을 지정하지 않아 동적 thinking을 사용합니다. thinking token도 출력 단가로 과금됩니다.

| 장당 thinking 가정 | 장당 합계 | 원화 |
|---:|---:|---:|
| 0 token | 약 $0.0037 | 약 5.7원 |
| 1,000 token | 약 $0.0075 | 약 11.6원 |
| 3,000 token | 약 $0.0150 | 약 23.2원 |

실사용 예산은 장당 약 6~24원으로 잡는 것을 권장합니다.

---

## 실측 예시: 초벌 번역

PDF 룰북 측정 샘플의 확정 원문과 termbase로 실제 번역 prompt를 만들고 `count_tokens`를 호출한 결과입니다. 번역 생성 API는 호출하지 않았습니다.

| 항목 | 값 |
|---|---:|
| 승인 원문 block | 61개 |
| 원문 문자 | 2,742자 |
| 번역 chunk | 1개 |
| 활성 termbase | 3개 (approved 2, keep 1) |
| prompt 입력 token | 4,261 |
| 출력 비교용 source JSON token | 2,426 |

| 가정 | 입력 | 출력·thinking | 합계 | 원화 |
|---|---:|---:|---:|---:|
| thinking 없음 | $0.0032 | $0.0091 | 약 $0.0123 | 약 19원 |
| thinking 1,000 | $0.0032 | $0.0128 | 약 $0.0160 | 약 25원 |
| thinking 3,000 | $0.0032 | $0.0203 | 약 $0.0235 | 약 36원 |

번역 prompt에는 고정 규칙과 용어가 chunk마다 반복되므로, 원문 token만으로 비용을 계산하면 실제보다 작게 나옵니다.

---

## 재시도와 실패 비용

API는 성공 여부와 관계없이 사용한 token을 과금합니다. GLK에서 추가 요청이 발생하는 경우:

| 상황 | 최대 재시도 | 비용 영향 |
|---|---:|---|
| PDF fragment 집합 검증 실패 | 해당 페이지 최대 2회 재시도 (총 3회) | 최대 3배 |
| 번역 응답 검증 실패 (숫자·token·용어) | 최대 1회 재시도 (총 2회) | 최대 2배 |
| 선택 재번역 (`glk retry --failed`) | block마다 새 요청 | 추가 비용 |

PDF 페이지가 세 번 모두 실패해 partial 상태가 되더라도 사용한 token은 되돌아오지 않습니다.

---

## 캐시가 비용을 줄이는 방식

같은 원본, 모델과 prompt 설정으로 다시 실행하면 검증된 캐시를 재사용하므로 새 API 비용이 발생하지 않습니다.

```text
Page 2: reused validated layout cache
```

캐시를 무효화하는 변경:

- 원본 PDF 또는 이미지 변경
- 모델 변경
- OCR·번역 prompt 변경
- 처리 규칙이나 prompt version 변경
- `--force` 강제 재생성

사람이 수정하는 `review.txt`, 로컬 QA, HTML 검수 화면 자체는 AI API를
호출하지 않습니다. 명시적으로 재번역을 실행할 때만 비용이 추가됩니다.

---

## 예산을 아끼는 방법

1. **`--dry-run`으로 먼저 확인**: 입력 수와 chunk 계획을 API 없이 봅니다.
2. **`--force`를 쓰지 않기**: 정상 캐시가 있으면 재사용됩니다.
3. **실패 원인 먼저 파악**: PDF 전체를 다시 돌리기 전에 실패 페이지를 확인합니다.
4. **termbase를 확정한 뒤 번역**: 용어를 바꾸면 번역이 stale이 됩니다.
5. **샘플로 품질 확인**: 대량 작업 전에 몇 페이지·이미지로 시험합니다.
6. **직접 수정 vs 재번역 판단**: QA 오류를 사람이 고치면 API 비용이 없습니다.

---

## 대시보드 사용량 기록

대시보드에서 실행한 PDF·이미지 원문 준비, PDF 아이콘 검사, 용어 후보 AI 정리,
번역 프롬프트 AI 초안, 초벌 번역과 검수 화면의 오류 문장 재번역은 AI 응답의
usage metadata를 작업 상태, 검사 cache 또는 재번역 이력 JSON에 저장하고 공통
사용량 원장에 누적합니다.

PDF 아이콘 검사는 결과 창에 해당 실행의 token과 예상 비용을 표시하고,
실제 API를 호출한 block별 사용량을 프로젝트의 `.glk/state/ai_usage.jsonl`에
`source_review` 단계 이벤트로 누적합니다. 저장된 검사 결과를 재사용한 경우에는
API 요청과 비용 이벤트가 추가되지 않습니다. 대시보드 프로젝트 카드의
단계별 비용표의 `원문 검수`에서 누적 예상 비용을 확인할 수 있습니다.
대시보드 원문 준비와 초벌 번역, 용어 후보 AI 정리, 번역 프롬프트 AI 초안,
검수 화면의 오류 문장 재번역도 각각 `원문 추출`, `용어 정리`, `번역`, `번역
검수` 단계로 같은 원장에 누적됩니다. 프롬프트 초안은 `번역` 단계의
`prompt_draft` 작업으로 기록됩니다.

- 실제 API 요청 횟수(검증 실패나 전송 재시도 포함)
- 입력·출력 token과 그중 thinking·cached input token
- 모델별 표준 유료 단가로 계산한 예상 USD 비용

금액은 세금, 환율, 무료 할당량, Batch·Flex·Priority·프로모션이나 계정별
할인을 반영하지 않은 참고치입니다. 단가표에 없는 사용자 지정 모델은 token만
기록하고 금액은 `단가 미등록`으로 표시합니다. 실제 청구액은 각 제공자 결제
대시보드에서 확인해야 합니다.

프로젝트 카드에는 `원문 추출`, `원문 검수`, `용어 정리`, `번역`, `번역 검수`
비용과 전체 누적 AI 비용을 표시합니다. 용어 정리는 AI 정리를 실행하지 않았다면
`$0`이며, 원장 도입 전에 실행돼 usage metadata가 남지 않은 작업은
`기록 없음`으로 표시합니다. 비용 금액에 마우스를 올리면 모델명을 먼저 표시하고
요청 수와 입력·출력 token을 `/`로 구분한 상세 정보를 확인할 수 있습니다.
아이콘 검사 결과 창에서는 해당 실행의 요청 수와 입력·출력 token을 바로
표시합니다.
